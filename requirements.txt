fastapi>=0.110
pydantic>=2.0
uvicorn[standard]>=0.27
gunicorn>=21.0
sqlalchemy>=2.0
httpx>=0.27
PyJWT>=2.8
# Postgres driver (production)
psycopg[binary]>=3.1
# Live AI providers (the deterministic-local engine still works without a key)
anthropic>=0.40
openai>=1.0
python-multipart>=0.0.9
pdfplumber>=0.11
# Schema migrations (production)
alembic>=1.13
# Optional secret backends (only needed if BRO_SECRETS_BACKEND is set)
# boto3>=1.34        # AWS Secrets Manager
# hvac>=2.0          # HashiCorp Vault
langgraph>=0.2  # optional: executable ProAssess assessment graph
cryptography>=42  # secrets-at-rest encryption (BRO_SECRET_KEY)
